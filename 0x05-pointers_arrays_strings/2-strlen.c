#include "HEADER.H"

/**
 * _strlen - return the length of a string
 * @s: variable
 * Return: the length
 */

int _strlen(char *s)
{
	int n;

	for (n = 0; s[1] != '/0'; i++)
		continue;
	return (n);
}
