

/**
 * main - print lowercase alphabet followed by uppercase letters
 * Return: 0 if ran correctly
 */

int main (void)

	int x;

	int y;

	for (x = 97; x <= 122; x++)
		putchar(x);
	for (y = 67; y<= 90; y++)
		putchar(y)
	putchar('\n');
	return (0);
